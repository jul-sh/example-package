# Produces multiple artifacts, in nested paths.
command = [
  "bash",
  "-c",
  "touch one_flat.multi && touch two_flat.multi && mkdir nested && touch nested/three_nested.multi",
]
# Wildcard that resolves to the files generated by the command above.
artifact_path = "*.multi"
